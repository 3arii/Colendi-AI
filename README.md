# Install Requirements

`pip3 install -r requirements.txt`

# Run the Code

`python3 main.py PDFs/{The Name of Your PDF}.pdf`

# Further Cleaning with Unstructured

1. Change the input file from the 90th line of the `clean_text.py` to your desired input file.
2. `python3 clean_text.py`
